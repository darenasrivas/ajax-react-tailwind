const SERVER_URL = 'https://juanda.certweb.infenlaces.com'
const CATEGORIES_URL = `${SERVER_URL}/categories`
const DISHES_URL = `${SERVER_URL}/dishes`
const RESTAURANTS_URL = `${SERVER_URL}/restaurants`

// Objeto API que se utiliza para la llamada a la API

const API = {
  CATEGORIES_URL,
  DISHES_URL,
  RESTAURANTS_URL,
}

export default API
